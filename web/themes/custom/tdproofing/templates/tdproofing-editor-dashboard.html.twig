<?php

/**
 * Implements hook_menu_local_tasks_alter().
 */
function custom_redirect_menu_local_tasks_alter(&$local_tasks, $route_name, &$cacheability) {
  // Leave empty — or remove the hook entirely.
}