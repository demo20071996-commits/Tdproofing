{{ attach_library('custom_redirect/dependent_dropdowns') }}

<!-- <div class="dashboard-default">
  <h2>Fallback Dashboard</h2>

  <p>User: {{ user.getDisplayName() }}</p>
  <p>Company: {{ company_data.company }}</p>
  <p>Journal: {{ company_data.journal }}</p>
  <p>Role: {{ company_data.role }}</p>
</div>--  >

<div class="dashboard-wrapper">

  <h2>Dashboard</h2>

  <!-- Top Cards -->
  <div class="dashboard-row">

    <div class="card-box">
      <div class="card-icon">👤</div>
      <div class="card-title">Welcome</div>
      <div class="card-value">2500</div>
    </div>

    <div class="card-box">
      <div class="card-icon">⏱️</div>
      <div class="card-title">Average Time</div>
      <div class="card-value">123.50</div>
    </div>

    <div class="card-box">
      <div class="card-icon">☁️</div>
      <div class="card-title">Collections</div>
      <div class="card-value">1805</div>
    </div>

    <div class="card-box">
      <div class="card-icon">💬</div>
      <div class="card-title">Comments</div>
      <div class="card-value">54</div>
    </div>

  </div>



  <!-- Chart Section -->
  <div class="chart-box">
    <h3>Extra Area Chart</h3>
    <canvas id="chart"></canvas>
  </div>

</div>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

<script>
var ctx = document.getElementById('chart').getContext('2d');
new Chart(ctx, {
  type: 'bar',
  data: {
    labels: ['Jan','Feb','Mar','Apr','May','Jun'],
    datasets: [{
      label: 'Dataset 1',
      data: [20, 35, 40, 60, 80, 75]
    }]
  }
});
</script>
